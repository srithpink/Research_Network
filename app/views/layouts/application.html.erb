<!DOCTYPE html>
<html>
  <head>
    <title>Abcd</title>
    <p class="notice">hello<%= notice %></p>
       <p class="alert">hw r u<%= alert %></p>
        <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <body>
  <div id="user_nav">
    <% if user_signed_in? %>
        Signed in as <%= current_user.name %>. This cannot be cheese?
        <%= link_to 'Sign out', destroy_user_session_path(:authenticity_token => form_authenticity_token()) , :method => 'delete'%>
    <% else %>
        <%= link_to 'Register', new_user_registration_path %> or <%= link_to 'Sign in', new_user_session_path %>
    <% end %>
</div>
    <%= yield %>
  </body>
</html>
