<% if !current_user.following?(@user) %>
    <%= render '/components/follow_button', :user => User.find_by_name(params[:id]) %>
<% else %>
      <%= render '/components/unfollow_button', :user => User.find_by_name(params[:id]) %>
<% end %>